<Window x:Class="DataGrid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataGrid"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Margin="20">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="txtNev" Width="160" Margin="0,0,10,0"/>
            <TextBox x:Name="txtKor" Width="70" Margin="0,0,10,0"/>
            <Button Content="Hozzáad" Click="Button_Click"/>
        </StackPanel>
        <DataGrid x:Name="dgSzemelyek" AutoGenerateColumns="False" Height="220">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Név" Binding="{Binding Nev}" Width="*"/>
                <DataGridTextColumn Header="Kor" Binding="{Binding Kor}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</Window>
